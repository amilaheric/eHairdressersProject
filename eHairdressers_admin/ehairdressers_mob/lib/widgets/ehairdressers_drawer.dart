import 'package:flutter/cupertino.dart';

class ehairdressersDrawer extends StatelessWidget {
  const ehairdressersDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
